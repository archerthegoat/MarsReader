# Mars Reader 开发路径图

> 文档性质：Mars Reader 的人工批准开发路线，不是自动同步的状态报告。
>
> 当前目标：Vercel 公网部署、公开只读浏览、固定超级账号管理个人数据。
>
> 批准状态：已批准，按本文执行路线图集成分支工作。

## 1. 产品定位与目标

Mars Reader 定位为“个人主控、公开浏览”的 RSS/资讯阅读器。

核心目标：

- 让所有者在手机、电脑和平板等不同设备上访问同一份文章和阅读记录。
- 让访客可以通过公网看到 Mars Reader 的品牌、界面、RSS 阅读体验、热门页面和已有公开内容。
- 保护所有者的信息源配置、阅读记录、服务器端 AI 配额和数据库写入权限。
- 当前只服务一个固定超级账号，不建设额外的账号体系。

产品边界：

- 公开部署不等于开放注册。
- 游客可以公开浏览，但不能修改服务器数据。
- 只有固定超级账号可以执行管理、刷新、写入和服务器端 AI 操作。
- 热门功能第一阶段保留，不重做其展示和聚合逻辑。

## 2. 当前事实与约束

当前项目具备以下结构：

- Express Web 服务和 API 集中在 `server.js`。
- SQLite 数据库使用 `data/qmreader.sqlite`。
- RSS 缓存和源状态使用 `data/cache.json`、`data/state.json` 等本地文件。
- 服务启动时会建立刷新计时器和后台任务。
- 服务器端 DeepSeek key 只应由服务端使用。
- 浏览器端 AI provider/API key 保存在浏览器本地，不写入 SQLite。

Vercel 适合作为公网 Web 和 API 运行平台，但不保证 Function 本地文件系统是跨请求、跨实例的永久存储。因此不能直接把当前本地 SQLite、JSON 缓存和常驻刷新进程原样作为生产架构。

参考证据：

- `server.js:3631-3638`：启动 Web 服务并建立刷新任务。
- `lib/store.js:4-15`：使用本地 SQLite 文件。
- `lib/fetcher.js:14-18`：使用本地 RSS 缓存和状态文件。
- Vercel Express 文档：Express 会作为 Vercel Function 运行。
- Vercel SQLite 文档：Vercel Function 的本地文件系统不能作为可靠的永久 SQLite 存储。
- Vercel Cron 文档：定时任务需要通过 HTTP Function 触发，并需要处理超时、重复调用和并发。

## 3. 已确定的部署形态

生产部署采用：

- Vercel 作为公网 Web/API 平台。
- 外部托管关系型数据库作为持久化数据库。
- 必要时使用 Redis 或其他托管 KV 作为任务锁、限流和短期队列。
- Vercel Cron 或外部调度器触发 RSS 刷新任务。
- 固定超级账号作为 Mars Reader 的唯一写入身份。
- 游客可以公开访问只读内容。
- 不提供普通注册入口。

建议的部署模式配置名：

```text
ACCESS_MODE=owner_public
REGISTRATION_ENABLED=0
```

以上配置名是实现建议，实际接入时可以根据代码结构调整，但必须保持“公开只读、超级账号写入”的语义。

## 4. 公开访问范围

游客无需登录即可：

- 查看信息源列表。
- 浏览文章和文章详情。
- 查看已有公开翻译、改写、点评和文章对话。
- 查看热门、精选和其他公开聚合页面。
- 查看公开的品牌、导航和产品体验。

游客不能：

- 修改信息源或源启用状态。
- 触发全量刷新或任意高成本抓取。
- 写入所有者的阅读状态、评论、标注或收藏。
- 使用服务器端 DeepSeek key。
- 修改或删除公开内容。
- 创建 Mars Reader 账号。

公开读取接口必须与写入接口明确分离，不能只依赖前端隐藏按钮。

## 5. 固定超级账号与身份校验

固定超级账号复用现有管理员账号初始化机制：

- 使用 `ADMIN_EMAIL`、`ADMIN_PASSWORD`、`ADMIN_NAME` 初始化唯一超级账号。
- 关闭公开注册接口。
- 超级账号登录后获得安全 session cookie。
- 所有者写操作统一执行超级账号权限检查。
- 未配置超级账号凭据时，生产部署失败关闭，不得意外回退成开放注册。
- 不要求接入第三方身份平台作为第一阶段前置条件。
- 不在前端保存管理员密码或服务器 key。
- 开启 HTTPS、`Secure` Cookie、登录限流和会话失效控制。

必须重点保护的接口类别：

- 源启用/禁用。
- 单源刷新和全量刷新。
- 标题翻译和文章改写。
- 服务器端 AI 模型调用。
- 投稿审核和删除。
- 文章、评论、标注、对话和个人状态写入。
- 原文抓取、favicon 抓取等可能触发外部请求或写入缓存的接口。
- Cron 刷新入口。

## 6. 数据持久化方案

结构化数据迁移到托管关系型数据库。

迁移范围包括：

- 文章和信息源元数据。
- 翻译、改写、点评、标注和文章对话。
- 超级账号、session 和个人阅读状态。
- 热门统计和其他聚合数据。
- 信息源启用状态和刷新状态。
- 任务状态、错误状态和最近成功时间。

数据库供应商在实现阶段通过验证后确定，不在本路线图中预先锁定为单一产品。

本地文件的处理原则：

- 不再依赖 Vercel Function 本地文件保存生产状态。
- RSS 缓存、源状态和 favicon 缓存迁移到数据库、对象存储或托管 KV。
- 原有 SQLite 和 `data/` 文件作为迁移源和回滚备份保留。
- 不把运行时数据库、缓存、session 或任何 key 提交到 Git。

## 7. 刷新、队列与 AI 任务

当前常驻 `setInterval`、`setTimeout` 和子进程 worker 不直接作为 Vercel 生产后台机制。

刷新机制改为：

- 由 Vercel Cron 或外部调度器调用受保护的刷新 API。
- 使用 `CRON_SECRET` 或等效服务密钥保护 Cron 入口。
- 每次任务只处理有界数量的信息源。
- 任务状态写入托管数据库。
- 使用数据库锁或托管 KV 锁防止重复执行。
- 任务设计为幂等，重复触发不会重复写入或破坏数据。
- 单个源失败不影响其他源，并保留最近一次成功缓存。
- 超时任务可以在下一次调度中继续或重试。

服务器端 AI：

- 服务端 DeepSeek 只保留给超级账号或受保护的后台翻译、改写任务。
- 现有 AI 伴读、文章聊天、模型列表、连接测试和浏览器 AI 配置保持原样。
- 本次二开不改变 AI 伴读的登录要求、请求方式、浏览器存储方式或数据保存方式。
- 不新增未登录游客使用 AI 伴读的能力。
- 现有服务器端翻译和改写任务仍需记录状态、失败原因和是否保存结果。

## 8. 状态与失败处理矩阵

| 场景 | 预期行为 |
|---|---|
| 游客读取正常 | 返回公开内容和公开聚合 |
| 游客尝试写入 | 返回未授权或禁止，不改变数据库 |
| 游客尝试刷新 | 返回禁止，不触发外部抓取 |
| 超级账号登录失败 | 不建立 session，不泄露账号是否存在 |
| 超级账号凭据缺失 | 生产部署失败关闭 |
| 托管数据库不可用 | 公开内容返回明确错误或最近可用缓存，不写入本地临时数据库 |
| RSS 源失败 | 保留最近一次成功数据，记录源级错误和时间 |
| Cron 重复调用 | 由锁和幂等机制避免重复刷新 |
| Cron 超时 | 记录未完成状态，下一次任务可恢复或重试 |
| 服务端翻译或改写 key 缺失 | 对应超级账号或后台任务明确失败，不回退为游客可用 |
| AI 伴读请求 | 保持现有登录和浏览器配置行为，不新增游客访问路径 |
| 单个 AI 结果不完整 | 不保存不完整结果，保留原有结果 |
| Vercel Function 冷启动 | 请求可恢复，不能依赖进程内状态作为唯一事实 |
| 数据迁移失败 | 不切生产流量，保留原 SQLite 服务作为回滚源 |
| 热门数据暂时没有新互动 | 继续展示已有聚合，不修改热门数据结构 |

## 9. 浏览器与视觉验收清单

### 验收构建与起始状态

- 构建：批准后的路线图集成分支对应的 Vercel Preview Deployment。
- 生产 URL：完成最终决策门后再切换，不在 Preview 阶段作为生产事实。
- 浏览器：内置 Browser。
- 标签页：只使用一个可复用标签页。
- 桌面视口：1440×1000，缩放 100%。
- 移动视口：390×844，缩放 100%。
- 测试数据：至少包含一个可读取文章、一个已有公开翻译或改写、一个热门聚合结果、一个最近失败或过期的信息源状态。
- 超级账号凭据：从部署环境或密码管理器读取，不写入测试记录。

### 公开游客流程

1. 打开生产或 Preview URL。
2. 检查品牌、页面标题、favicon 和首屏布局。
3. 浏览信息源列表和文章列表。
4. 打开一篇文章，检查正文、图片、链接和来源信息。
5. 打开热门页面或热门区域。
6. 切换桌面和移动视口。
7. 使用鼠标、键盘 Tab、Enter、Escape 和滚动完成主要导航。
8. 检查加载中、空数据、部分源失败、旧缓存和请求失败状态。
9. 尝试进入管理路径，确认不会看到管理数据或写入控件。
10. 尝试触发刷新、翻译、改写或评论，确认界面要求登录或明确拒绝。
11. 检查浏览器 Console 无未处理异常。
12. 检查 Network 中没有下发服务器 DeepSeek key、数据库凭据或管理员 session。

### 超级账号流程

1. 在同一标签页登录超级账号。
2. 确认登录后显示正确身份。
3. 执行一次受控源刷新。
4. 检查刷新状态、成功状态、失败状态和最近更新时间。
5. 执行一次手动标题翻译或改写。
6. 检查结果完成后才会显示，不完整结果不会覆盖旧结果。
7. 修改一项允许的个人阅读状态。
8. 刷新页面，确认个人状态仍然存在。
9. 访问管理页面，确认源配置和任务状态可见。
10. 登出后再次访问管理路径，确认写入能力立即消失。

### 跨设备数据流程

1. 在桌面视口登录超级账号并标记一篇文章。
2. 刷新页面，确认记录仍然存在。
3. 切换到移动视口或另一台设备。
4. 登录同一超级账号。
5. 确认文章状态、个人配置和服务端保存内容一致。
6. 确认浏览器 AI 配置仍按现有本地存储方式工作，不被迁移到服务器数据库。

### 网络与运维检查

- 检查所有写接口都返回正确的 401/403/成功状态。
- 检查 Cron 入口没有公开暴露可执行能力。
- 检查重复 Cron 不会启动重复刷新。
- 检查任务超时后不会把系统标记为永久运行中。
- 检查托管数据库、KV/队列和 Vercel Function 的日志。
- 记录每项检查的通过、失败、阻塞或未测试状态。
- 测试结束后退出超级账号，不在浏览器中保留临时管理员状态。

## 10. 迁移与回滚

迁移前：

- 备份 `data/qmreader.sqlite`、`data/cache.json`、`data/state.json` 和 `data/favicons.json`。
- 保存当前提交 `616ff70` 作为可回滚基线。
- 记录当前信息源数量、文章数量、公开资产数量和超级账号状态。
- 不删除原 SQLite 和原运行目录。

迁移过程：

1. 创建托管数据库和必要的 KV/队列资源。
2. 实现数据访问层适配器。
3. 将 SQLite 数据导入托管数据库。
4. 执行只读比对，确认文章、源、用户、资产和统计数量一致。
5. 将刷新任务改为 Cron/队列模式。
6. 在 Preview 环境验证公开读取和超级账号写入。
7. 通过最终决策门后再切生产流量。

回滚：

- 保留当前 Node/SQLite 服务作为回滚版本。
- 若托管数据库、Cron 或权限边界不稳定，停止生产切换。
- 回滚不删除托管数据库或原始备份。
- 不重新开放注册作为临时补救。
- 回滚后重新检查服务器 AI key、session 和写接口权限。

## 11. 实施阶段

### 阶段一：部署适配设计

- 确认 Vercel Function 入口、静态资源路径和 API 路由映射。
- 确认托管数据库候选和迁移成本。
- 确认 Cron 频率、时区、锁和任务批次。
- 输出数据迁移映射和回滚方案。

### 阶段二：持久化层迁移

- 抽象当前 SQLite 数据访问接口。
- 实现托管数据库适配器。
- 迁移全局文章、源、资产、超级账号和任务状态。
- 保留 SQLite 读取和导出能力作为迁移期回滚工具。
- 完成数据一致性测试。

### 阶段三：Vercel 运行时适配

- 将 Express 应用导出为 Vercel Function。
- 将静态资源交给 Vercel `public/` 目录服务。
- 移除对常驻进程和进程内状态的关键依赖。
- 将刷新、翻译和改写任务拆为受保护的 HTTP/Cron 任务。
- 完成函数超时、并发和错误恢复测试。

### 阶段四：固定超级账号与公开只读权限

- 关闭注册。
- 增加明确的超级账号权限中间件。
- 盘点所有非 GET 接口和高成本读取接口。
- 限制游客为公开只读。
- 限制服务器 DeepSeek 只供超级账号和受保护后台使用。
- 保留热门展示。

### 阶段五：Vercel Preview 与生产验收

- 在 Preview 环境完成浏览器验收清单。
- 验证跨设备个人数据同步。
- 验证刷新、服务器端翻译/改写、错误和回滚状态。
- 完成安全、性能和成本检查。
- 经过最终决策门后再部署生产。

## 12. 不在当前范围内

- 不提供普通注册入口。
- 不在游客侧开放服务器 DeepSeek。
- 不新增游客 AI 伴读或游客 BYOK 访问模式。
- 不改变现有 AI 伴读、文章聊天、模型列表、连接测试和浏览器 AI 配置行为。
- 不重做热门逻辑。
- 不改变当前 RSS 和播客来源范围。
- 不把 Vercel 本地文件系统当作数据库。
- 不在没有迁移和回滚证据前删除当前 SQLite 数据。

## 13. 未决风险

- 托管数据库供应商尚未确定。
- Vercel Hobby Cron 频率可能不足以保持当前刷新间隔。
- RSS 源和 RSSHub 的外部稳定性仍然存在风险。
- DeepSeek 服务端调用需要设置成本和失败边界。
- Vercel Preview、Production 和 Cookie 域策略需要单独验证。
- 数据迁移期间需要避免 RSS 缓存与数据库状态不一致。
- Vercel Function 的并发模型可能暴露当前内存态任务状态的隐含假设。

## 14. 分支与批准流程

- 本路线图已在 `codex/marsreader-roadmap` 集成分支上维护。
- 每个阶段从路线图集成分支创建独立子分支。
- 每个子分支只实现一个阶段。
- 阶段完成后先通过测试、数据检查和对抗性审查，再合并回路线图集成分支。
- 所有阶段完成后执行完整集成验收。
- 未经过最终决策门，不合并到 `main`，不推送生产，不切换线上流量。
- 本路线图文件只按用户批准的文字更新，不根据代码自动同步。

## 15. 批准状态

本文内容已获用户批准，作为 `codex/marsreader-roadmap` 集成分支的路线图依据。
